# Habit Tracker Frontend

React + Vite + Apollo client for the Django + GraphQL Habit Tracker.
Includes guest mode via localStorage and authenticated mode via Django session auth (credentials included).
